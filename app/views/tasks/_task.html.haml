= form_for task, remote: true do |f|
  = f.check_box :complete
  = f.submit "Update"
  = f.label :complete, task.name
  = link_to "remove", task, method: :delete, data: {confirm: "Have you really done it?"}
